import type { MetaRecord } from 'nextra'

const meta: MetaRecord = {
  prerequisites: 'Prerequisites',
  'local-setup': 'Local setup',
  'first-admin': 'First admin account',
  'project-tour': 'Project tour',
}

export default meta
