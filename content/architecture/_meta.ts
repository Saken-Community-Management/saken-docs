import type { MetaRecord } from 'nextra'

const meta: MetaRecord = {
  overview: 'Overview',
  frontend: 'Frontend (Next.js)',
  backend: 'Backend & MCP',
  'auth-rls': 'Auth & RLS wiring',
  'multi-tenant': 'Multi-tenant isolation',
}

export default meta
