import type { MetaRecord } from 'nextra'

const meta: MetaRecord = {
  'server-actions': 'Server actions',
  'backend-endpoints': 'saken-server endpoints',
  'mcp-tools': 'saken-mcp tools',
  delivery: 'Email & WhatsApp delivery',
}

export default meta
